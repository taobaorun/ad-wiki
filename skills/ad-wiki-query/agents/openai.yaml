interface:
  display_name: "AD Wiki Query"
  short_description: "Query one AD Wiki with cited, read-only answers"
  default_prompt: "Use $ad-wiki-query to answer a question from the current AD Wiki with citations."
policy:
  allow_implicit_invocation: true
